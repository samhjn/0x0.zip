# 0x0.zip - Project Ideas Brainstorm

> 当前状态：单文件纯前端应用，功能是生成自定义二进制内容的 ZIP 文件。  
> 以下是可以继续做的方向和具体功能点。

---

## 一、核心功能增强

### 1. 多文件打包
- 允许用户在一个 ZIP 中添加多个文件，每个文件可以有不同的长度、模式和文件名
- 提供"添加文件 / 删除文件"按钮，动态增减文件配置行

### 2. 文件名自定义
- 目前固定生成 `data.bin`，允许用户自定义 ZIP 内部的文件名和扩展名
- 输出的 ZIP 文件名也应可自定义（目前固定 `data.zip`）

### 3. 大小单位选择器
- 当前只能输入字节数，增加 KB / MB / GB 单位下拉框
- 例如输入 `10` + 选择 `MB` = 生成 10MB 的文件

### 4. 更多数据生成模式
- **递增模式**：`00 01 02 ... FF 00 01 ...` 循环递增
- **递减模式**：`FF FE FD ...` 循环递减
- **文本重复模式**：输入一段文本（如 "Hello"），重复填充到指定长度
- **自定义 JavaScript 表达式**：`(i) => i % 256`，高级用户可以用函数定义每个字节

### 5. 压缩选项
- 压缩级别选择（1-9）
- 支持"不压缩 / 仅存储"模式（STORE vs DEFLATE）
- 显示压缩前后的大小对比和压缩率

---

## 二、用户体验改进

### 6. 数据预览
- 生成前显示前 N 个字节的十六进制预览（类似 hex editor）
- 可选的 ASCII 侧边栏显示

### 7. 进度条
- 大文件生成时显示进度条和预计剩余时间
- 使用 Web Worker 避免阻塞 UI

### 8. 深色模式
- 添加明/暗主题切换
- 跟随系统偏好 `prefers-color-scheme`

### 9. 响应式设计优化
- 移动端适配，当前在小屏幕上布局可以更紧凑
- 添加 `<meta name="viewport">` 标签（目前缺失）

### 10. 分享链接按钮
- 根据当前配置生成可分享的 URL（利用已有的 query string 功能）
- 一键复制到剪贴板

### 11. 历史记录
- 使用 `localStorage` 保存最近生成的配置
- 可以快速重新生成之前的文件

---

## 三、高级功能

### 12. 文件哈希显示
- 生成后显示文件的 MD5 / SHA-1 / SHA-256 哈希值
- 方便用户验证下载的文件完整性

### 13. 拖拽上传再打包
- 允许用户拖拽现有文件到页面
- 将用户文件与生成的二进制文件一起打包成 ZIP

### 14. 在线解压预览
- 反向功能：上传 ZIP 文件，在页面上查看内容（文件列表、大小、hex 预览）
- 形成"生成 + 查看"的完整闭环

### 15. 批量生成
- 一次生成多个不同配置的 ZIP 文件
- 或生成一个包含多种模式文件的测试数据集

### 16. 密码保护 ZIP
- JSZip 本身不支持加密，但可以引入其他库（如 `minizip-asm.js`）
- 支持 AES-256 加密的 ZIP 文件生成

---

## 四、工程化改进

### 17. 离线支持 (PWA)
- 添加 Service Worker 和 manifest.json
- 注册为 PWA，CDN 资源本地缓存后可完全离线使用

### 18. 本地化 (i18n)
- 支持多语言（至少中/英文）
- 根据浏览器语言自动切换

### 19. SEO 和元数据
- 添加 Open Graph 标签（分享到社交媒体时有预览）
- 添加 favicon
- 添加 `viewport` meta 标签
- 结构化数据 (JSON-LD)

### 20. 无障碍 (Accessibility)
- 添加 ARIA 标签
- 键盘导航支持
- 屏幕阅读器友好

### 21. 项目结构重构
- 将 CSS、JS 分离到独立文件
- 引入轻量构建工具（如 Vite）管理依赖，替代 CDN 引用
- 添加单元测试（用 Vitest 测试数据生成逻辑）

### 22. 添加 README
- 项目当前没有 README.md
- 添加项目简介、使用说明、API 文档（query string 参数）、截图

---

## 五、有趣的拓展方向

### 23. ZIP 炸弹演示/教育
- 既然项目名叫 0x0.zip，可以增加一个教育性页面
- 解释 ZIP 炸弹的原理（嵌套压缩、重复数据的高压缩比）
- 提供安全的小规模演示（如生成一个压缩比极高的文件）

### 24. 二进制数据可视化
- 将生成的数据以色块/热力图形式可视化
- 不同字节值对应不同颜色，直观看出数据分布

### 25. 命令行工具版本
- 提供 Node.js CLI 版本，方便在脚本/CI 中使用
- `npx 0x0zip --length 1024 --pattern FF00`

### 26. 浏览器扩展
- Chrome/Firefox 扩展，右键菜单快速生成测试文件
- 在开发者工具面板中集成

---

## 优先级建议

| 优先级 | 功能 | 理由 |
|--------|------|------|
| P0 | viewport meta 标签 | 一行代码，修复移动端显示 |
| P0 | 文件名自定义 | 用户需求明确，实现简单 |
| P1 | 大小单位选择器 | 大幅提升易用性 |
| P1 | 深色模式 | 现代 Web 标配 |
| P1 | 添加 README | 开源项目基本要求 |
| P2 | 多文件打包 | 功能差异化 |
| P2 | 数据预览 | 提升信任感 |
| P2 | 压缩选项 | 高级用户需要 |
| P3 | PWA 离线支持 | 锦上添花 |
| P3 | ZIP 炸弹教育页面 | 有趣且与项目名契合 |
